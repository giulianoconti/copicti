import "./styles.css";

const IsError = () => {
  return (
    <div className="isError-box">
      <h2 className="isError-box-text">Something went wrong.</h2>
      <h2 className="isError-box-text">Please try again later.</h2>
    </div>
  );
};

export default IsError;
